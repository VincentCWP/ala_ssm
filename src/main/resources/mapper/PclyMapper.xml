<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.java.mapper.PclyMapper" > 
  
  
  <select id="selectLytype" resultType="pcly">
  	SELECT * FROM pcly GROUP BY lytype
  	</select>
  <select id="selectLy" resultType="pcly">
  	SELECT * FROM pcly
  	</select>
</mapper>